name: Dev — Frontend lint, build, GHCR, tracker

# Single job (Windows-safe): allocate → lint → build/push → report.
# Avoids cross-job GITHUB_OUTPUT propagation bugs on Windows PowerShell.
on:
  push:
    branches: [dev]
    paths: ['frontend/**']
  workflow_dispatch:
    inputs:
      fail_lint:
        description: 'Force lint failure (smoke-test failed deploy reporting)'
        required: false
        type: boolean
        default: false

jobs:
  ci:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    defaults:
      run:
        shell: powershell
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Allocate build in DeployTrack
        id: allocate
        env:
          TRACKER_URL: ${{ secrets.TRACKER_URL }}
          TRACKER_TOKEN: ${{ secrets.CICD_WEBHOOK_SECRET }}
          PROJECT_ID: ${{ secrets.TRACKER_PROJECT_ID }}
          IMAGE_REPO: ${{ secrets.IMAGE_REPOSITORY }}
        run: |
          $ErrorActionPreference = 'Stop'
          $utf8 = New-Object System.Text.UTF8Encoding $false
          function Add-RunnerEnv([string]$Line) {
            [System.IO.File]::AppendAllText($env:GITHUB_ENV, $Line + [Environment]::NewLine, $utf8)
          }

          $body = @{
            project_id       = [int]$env:PROJECT_ID
            commit_sha       = '${{ github.sha }}'
            branch           = 'dev'
            image_repository = $env:IMAGE_REPO
            component        = 'frontend'
          } | ConvertTo-Json -Compress

          $headers = @{ 'Content-Type' = 'application/json' }
          if ($env:TRACKER_TOKEN) {
            $headers['Authorization'] = "Bearer $($env:TRACKER_TOKEN)"
          }

          $resp = Invoke-RestMethod -Method POST -Uri "$($env:TRACKER_URL)/api/builds" -Headers $headers -Body $body
          $resp | ConvertTo-Json -Depth 6

          $buildId = [string]$resp.build.id
          $version = [string]$resp.version
          $buildNumber = [string]$resp.build_number
          $dockerImage = [string]$resp.'docker_image'
          if (-not $dockerImage -and $resp.build) {
            $dockerImage = [string]$resp.build.'docker_image'
          }
          if ([string]::IsNullOrWhiteSpace($buildId)) { throw 'allocate response missing build.id' }
          if ([string]::IsNullOrWhiteSpace($dockerImage)) { throw 'allocate response missing docker_image' }

          Add-RunnerEnv "BUILD_ID=$buildId"
          Add-RunnerEnv "VERSION=$version"
          Add-RunnerEnv "BUILD_NUMBER=$buildNumber"
          Add-RunnerEnv "DOCKER_IMAGE=$dockerImage"
          $env:BUILD_ID = $buildId
          $env:VERSION = $version
          $env:BUILD_NUMBER = $buildNumber
          $env:DOCKER_IMAGE = $dockerImage

          Write-Host "Allocated frontend build:"
          Write-Host "  build_id=$buildId"
          Write-Host "  docker_image=$dockerImage"

      - name: Optional intentional lint failure
        if: ${{ inputs.fail_lint == true }}
        run: |
          Write-Error "Intentional lint failure (fail_lint=true)"
          exit 1

      - name: Run repo lint scripts
        run: |
          $ErrorActionPreference = 'Stop'
          $lintDir = Join-Path $PWD 'ci/lint'
          $scripts = @()
          if (Test-Path $lintDir) {
            $scripts = @(Get-ChildItem -Path $lintDir -File -Filter '*.ps1' |
              Where-Object { $_.Name -notlike '*.disabled' } |
              Sort-Object Name)
          }
          if ($scripts.Count -gt 0) {
            foreach ($script in $scripts) {
              Write-Host ('=== Running ' + $script.Name + ' ===')
              & $script.FullName
              if ($LASTEXITCODE -ne 0) { throw ('Lint failed: ' + $script.Name) }
            }
          } else {
            Write-Host 'ci/lint empty or missing - using default frontend checks'
            if (Test-Path frontend) {
              Push-Location frontend
              try {
                if (Test-Path package-lock.json) { npm ci } else { npm install }
                $pkg = Get-Content package.json -Raw
                if ($pkg -match '"lint"\s*:') {
                  npm run lint
                  if ($LASTEXITCODE -ne 0) { throw 'npm run lint failed' }
                } else {
                  Write-Host 'No npm lint script; skipping'
                }
              } finally {
                Pop-Location
              }
            } else {
              throw 'frontend/ missing - cannot run default frontend lint'
            }
          }

      - name: Report failed dev deployment (lint)
        if: failure() && steps.allocate.outcome == 'success'
        env:
          TRACKER_URL: ${{ secrets.TRACKER_URL }}
          TRACKER_TOKEN: ${{ secrets.CICD_WEBHOOK_SECRET }}
        run: |
          $ErrorActionPreference = 'Stop'
          if (-not $env:BUILD_ID) { throw 'BUILD_ID missing after allocate' }
          $headers = @{ 'Content-Type' = 'application/json' }
          if ($env:TRACKER_TOKEN) {
            $headers['Authorization'] = "Bearer $($env:TRACKER_TOKEN)"
          }
          $failBody = @{
            build_id    = [int]$env:BUILD_ID
            environment = 'dev'
            status      = 'failed'
            deployed_by = 'github-actions'
          } | ConvertTo-Json -Compress
          try {
            Invoke-RestMethod -Method POST -Uri "$($env:TRACKER_URL)/api/deployments" -Headers $headers -Body $failBody | Out-Null
          } catch {
            Write-Warning "Failed to report failed lint deploy: $_"
          }

      - name: Login to GHCR
        env:
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          $ErrorActionPreference = 'Stop'
          $utf8 = New-Object System.Text.UTF8Encoding $false
          $user = [string]$env:GHCR_USERNAME
          $token = [string]$env:GHCR_TOKEN
          if ([string]::IsNullOrWhiteSpace($user)) { throw 'GHCR_USERNAME secret is empty' }
          if ([string]::IsNullOrWhiteSpace($token)) { throw 'GHCR_TOKEN secret is empty' }
          $user = $user.Trim()
          $token = $token.Trim()
          $cfg = Join-Path $env:RUNNER_TEMP 'docker-config'
          New-Item -ItemType Directory -Force -Path $cfg | Out-Null
          Set-Content -Path (Join-Path $cfg 'config.json') -Value '{}'
          [System.IO.File]::AppendAllText($env:GITHUB_ENV, "DOCKER_CONFIG=$cfg" + [Environment]::NewLine, $utf8)
          $env:DOCKER_CONFIG = $cfg
          $patFile = Join-Path $env:RUNNER_TEMP 'ghcr.pat'
          [System.IO.File]::WriteAllText($patFile, $token)
          try {
            cmd /c "docker login ghcr.io -u `"$user`" --password-stdin < `"$patFile`""
            if ($LASTEXITCODE -ne 0) { throw 'docker login ghcr.io failed' }
          } finally {
            Remove-Item -Force $patFile -ErrorAction SilentlyContinue
          }

      - name: Build and push frontend image
        id: push
        env:
          TRACKER_URL: ${{ secrets.TRACKER_URL }}
          TRACKER_TOKEN: ${{ secrets.CICD_WEBHOOK_SECRET }}
        run: |
          $ErrorActionPreference = 'Stop'
          if (-not $env:BUILD_ID) { throw 'BUILD_ID is empty after allocate' }
          if (-not $env:DOCKER_IMAGE) { throw 'DOCKER_IMAGE is empty after allocate' }
          if (-not (Test-Path './frontend')) { throw './frontend directory not found - check checkout working directory' }

          Write-Host "Building frontend image $($env:DOCKER_IMAGE) for build_id=$($env:BUILD_ID)"
          try {
            docker build -t "$env:DOCKER_IMAGE" ./frontend
            if ($LASTEXITCODE -ne 0) { throw 'frontend docker build failed' }
            docker push "$env:DOCKER_IMAGE"
            if ($LASTEXITCODE -ne 0) { throw 'frontend docker push failed' }
          } catch {
            $headers = @{ 'Content-Type' = 'application/json' }
            if ($env:TRACKER_TOKEN) { $headers['Authorization'] = "Bearer $($env:TRACKER_TOKEN)" }
            $failBody = @{
              build_id    = [int]$env:BUILD_ID
              environment = 'dev'
              status      = 'failed'
              deployed_by = 'github-actions'
            } | ConvertTo-Json -Compress
            try {
              Invoke-RestMethod -Method POST -Uri "$($env:TRACKER_URL)/api/deployments" -Headers $headers -Body $failBody | Out-Null
            } catch {
              Write-Warning "Failed to report failed deploy: $_"
            }
            throw
          }

      - name: Record successful dev deployment
        env:
          TRACKER_URL: ${{ secrets.TRACKER_URL }}
          TRACKER_TOKEN: ${{ secrets.CICD_WEBHOOK_SECRET }}
        run: |
          $ErrorActionPreference = 'Stop'
          $utf8 = New-Object System.Text.UTF8Encoding $false
          $body = @{
            build_id    = [int]$env:BUILD_ID
            environment = 'dev'
            status      = 'success'
            deployed_by = 'github-actions'
          } | ConvertTo-Json -Compress

          $headers = @{ 'Content-Type' = 'application/json' }
          if ($env:TRACKER_TOKEN) {
            $headers['Authorization'] = "Bearer $($env:TRACKER_TOKEN)"
          }

          $resp = Invoke-RestMethod -Method POST -Uri "$($env:TRACKER_URL)/api/deployments" -Headers $headers -Body $body
          $resp | ConvertTo-Json -Depth 6
          [System.IO.File]::AppendAllText($env:GITHUB_ENV, "DEPLOYMENT_ID=$($resp.id)" + [Environment]::NewLine, $utf8)
          $env:DEPLOYMENT_ID = [string]$resp.id

      - name: Summary
        run: |
          @"
          ### DeployTrack (dev CI — frontend)
          - **Version:** $env:VERSION-build$env:BUILD_NUMBER
          - **Frontend image:** ``$env:DOCKER_IMAGE``
          - **build_id:** $env:BUILD_ID
          - **deployment_id:** $env:DEPLOYMENT_ID
          "@ | Add-Content -Path $env:GITHUB_STEP_SUMMARY
